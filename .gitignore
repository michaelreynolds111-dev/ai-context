.env
*.key
secrets/
**/knowledge/raw/

# [IDENTITY] - belt and braces; the vault lives outside the repo entirely
household-vault/
projects/household/knowledge/
*.identifiers.md


# [CLINICAL] Power App moved to ai-workspace - never re-commit
projects/Power App/

# Claude data export - NEVER commit (contains full conversation history, SENSITIVE)
claude-export/
