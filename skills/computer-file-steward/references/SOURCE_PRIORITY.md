# SOURCE_PRIORITY.md — Source precedence and freshness model

When importing evidence into the registries or when resolving a fact during a
review, apply this precedence:

1. **Current live filesystem metadata** — existence and present state (most authoritative for "what is here now").
2. **Explicit protected-path and execution policy** — bounded workspace (ai-workspace README/SCOPE/AGENTS), live-system junction rules, Credential Rule.
3. **Current registries generated by this skill** — the four registries with provenance.
4. **Verified recovery and preservation manifests** — D: AI-System-Preservation Task-02/04, E: off-machine copies.
5. **Latest completion records** — verified archives/bundles/checksums.
6. **Current BUILD_STATE.md** — live tracker of phase/state.
7. **Historical investigation reports** — D: AI-System-Investigation (01–08, Task-03).
8. **Old architecture or master-plan intent** — BACKUP_AI_MASTER_BUILD_PLAN (advisory for placement).
9. **Filename-derived inference** — weakest; never decisive.

### v1.0.1 (Correction D): filename inference never establishes reproducibility
`README`/Markdown, `.tmp`, `.bak`, installer, cache, and backup names are
**never** decisive for classification. They cannot alone produce class C. Only an
explicit regeneration basis (package manifest, generating application/process,
identified regeneration process, authoritative duplicate/upstream, or an explicit
policy record) or an authoritative registry classification can. Metadata-only
uncertainty defaults to G.

## Freshness markers
- `HARD_POLICY` — governing rule (workspace root, live-system, credential rule, junctions).
- `OBSERVED` — direct read-only observation at a timestamp.
- `STALE` — known superseded or contradicted (e.g. LOC-009 claude-desktop-mcp).
- `UNKNOWN` — not yet determined.

## Conflict handling
Do not silently resolve contradictory facts. Examples carried in the built
registries (conflicts.json):
- **ai-context authoritative checkout**: WSL copy clean (LOC-016) vs Desktop copy
  dirty/uncommitted (LOC-017). UNRESOLVED — requires Michael; do not overwrite either.
- **Mongo data location**: Docker volume `librechat_librechat_mongo_data` vs
  `/home/michael/LibreChat/data-node/`. UNRESOLVED — confirm which chat-mongodb reads live.
- **Goose primary install**: Windows app v1.47.0 vs WSL CLI v1.47.0. UNRESOLVED — design decision.

## Machine-specific precedence clarifications
- **D:\Data is the LIVE pipeline; C:\HouseholdDataRaw\Data is a stale one-time
  snapshot** (GOTCHAS §11). A component is only "gone" when confirmed absent from
  BOTH drives. Treat C: snapshot as stale read-only; D: as authoritative for the legacy pipeline.
- **Git source-of-truth**: physical source is behind each junction/pointer;
  `live-systems\` is the AI-visible view, never the canonical copy. Prefer the WSL
  ai-context clone (clean) for build authority, but do not overwrite the dirty Desktop copy without Michael.
